const fs = require("fs"); let c = fs.readFileSync("src/apps/StressTensor/StressTensor.tsx", "utf8"); c = c.replace(/mainGroupRef.current.visible = viewMode === "cube";/g, ""); c = c.replace(/ellipsoidMeshRef.current.visible = viewMode === "ellipsoid";/g, ""); c = c.replace(/\{viewMode === "cube" && \(/g, "{true && ("); c = c.replace(/\{viewMode === "ellipsoid" && \([\s\S]*?\}\)/g, ""); c = c.replace(/\|\| viewMode !== "cube"/g, ""); c = c.replace(/setViewMode\("cube"\);/g, ""); c = c.replace(/<div className="flex p-1 bg-slate-950 rounded-lg border border-slate-800">[\s\S]*?<\/div>/, ""); c = c.replace(/, viewMode/g, ""); c = c.replace(/ && viewMode === "cube"/g, ""); fs.writeFileSync("src/apps/StressTensor/StressTensor.tsx", c);
